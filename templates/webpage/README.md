# Server side rendered webpage 

## Get started

To start up a development docker container run this:

``
docker-compose -f docker-compose.dev.yml up -d
``

Afterwards the webpage will be accessable on localhost:8100 on your machine.

## Run in production



