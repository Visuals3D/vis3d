{
  "watch": ["src", "swagger.yml", "public"],
  "ext": "ts",
  "ignore": ["src/**/*.spec.ts"],
  "exec": "ts-node ./src/index.ts",
  "events": {
    "start": "npm run generate:schema",
    "restart": "npm run generate:schema"
  }
}
